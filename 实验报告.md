# 实验报告：内存管理与基于地址空间的分时多任务

---

## 一、实验步骤

### 1.1 在内核中支持动态内存分配

**（1）添加依赖**

在 `os/Cargo.toml` 中增加伙伴分配器依赖：

```toml
buddy_system_allocator = "0.6"
```

在 `os/src/main.rs` 中引入 `alloc` 库：

```rust
extern crate alloc;
```

**（2）实现全局动态内存分配器**

新建 `os/src/mm/heap_allocator.rs`（本项目实际项目中使用 `heap.rs`）：

```rust
use buddy_system_allocator::LockedHeap;
use crate::config::KERNEL_HEAP_SIZE;

#[global_allocator]
static HEAP_ALLOCATOR: LockedHeap = LockedHeap::empty();

static mut HEAP_SPACE: [u8; KERNEL_HEAP_SIZE] = [0; KERNEL_HEAP_SIZE];

pub fn init_heap() {
    unsafe {
        HEAP_ALLOCATOR
            .lock()
            .init(core::ptr::addr_of!(HEAP_SPACE) as usize, KERNEL_HEAP_SIZE);
    }
}
```

同时添加分配失败处理：

```rust
#![feature(alloc_error_handler)]

#[alloc_error_handler]
pub fn handle_alloc_error(layout: core::alloc::Layout) -> ! {
    panic!("Heap allocation error, layout = {:?}", layout);
}
```

**（3）配置常量**

在 `os/src/config.rs` 中定义内核堆大小：

```rust
pub const KERNEL_HEAP_SIZE: usize = 0x10_0000; // 1MiB
```

---

### 1.2 实现虚拟地址与物理地址的基本定义

**（1）定义基础类型**

新建 `os/src/mm/address.rs`，定义四种地址类型作为 `usize` 的 newtype 包装：

| 类型 | 含义 |
|------|------|
| `PhysAddr` | 物理地址 |
| `VirtAddr` | 虚拟地址 |
| `PhysPageNum` | 物理页号 (4KB 对齐) |
| `VirtPageNum` | 虚拟页号 (4KB 对齐) |

**（2）实现类型间转换**

- `T → usize`：直接取内部值
- `usize → T`：通过 `From<usize>` trait
- 地址 ↔ 页号：`floor()` 向下取整、`ceil()` 向上取整、`page_offset()` 获取页内偏移

```rust
impl VirtAddr {
    pub fn floor(&self) -> VirtPageNum { VirtPageNum(self.0 / PAGE_SIZE) }
    pub fn ceil(&self) -> VirtPageNum  { VirtPageNum((self.0 - 1 + PAGE_SIZE) / PAGE_SIZE) }
    pub fn page_offset(&self) -> usize { self.0 & (PAGE_SIZE - 1) }
}
```

**（3）实现 VPN 三级索引**

Sv39 分页将 27 位 VPN 分解为三个 9 位索引（VPN[2], VPN[1], VPN[0]）：

```rust
impl VirtPageNum {
    pub fn indexes(&self) -> [usize; 3] {
        let mut vpn = self.0;
        let mut idx = [0usize; 3];
        for i in (0..3).rev() {
            idx[i] = vpn & 511;
            vpn >>= 9;
        }
        idx
    }
}
```

**（4）实现物理页的直接访问**

通过恒等映射直接访问物理内存：

```rust
impl PhysPageNum {
    pub fn get_bytes_array(&self) -> &'static mut [u8] {
        let pa: PhysAddr = self.clone().into();
        unsafe { core::slice::from_raw_parts_mut(pa.0 as *mut u8, 4096) }
    }
    pub fn get_mut<T>(&self) -> &'static mut T {
        let pa: PhysAddr = self.clone().into();
        unsafe { (pa.0 as *mut T).as_mut().unwrap() }
    }
}
```

**（5）实现虚拟页号范围迭代器**

定义 `VPNRange = SimpleRange<VirtPageNum>`，支持 `for vpn in range` 遍历连续虚拟页号。

---

### 1.3 定义页表项数据结构

**（1）添加 bitflags 依赖**

```toml
bitflags = "1.2.1"
```

**（2）定义页表项标志位**

新建 `os/src/mm/page_table.rs`，使用 `bitflags!` 宏定义 RISC-V Sv39 PTE 标志：

```rust
bitflags! {
    pub struct PTEFlags: u8 {
        const V = 1 << 0;  // Valid
        const R = 1 << 1;  // Readable
        const W = 1 << 2;  // Writable
        const X = 1 << 3;  // Executable
        const U = 1 << 4;  // User
        const G = 1 << 5;  // Global
        const A = 1 << 6;  // Accessed
        const D = 1 << 7;  // Dirty
    }
}
```

**（3）实现页表项**

```rust
#[derive(Copy, Clone)]
#[repr(C)]
pub struct PageTableEntry {
    pub bits: usize,
}

impl PageTableEntry {
    pub fn new(ppn: PhysPageNum, flags: PTEFlags) -> Self {
        PageTableEntry {
            bits: ppn.0 << 10 | flags.bits as usize,
        }
    }
    pub fn ppn(&self) -> PhysPageNum {
        (self.bits >> 10 & ((1usize << 44) - 1)).into()
    }
    pub fn flags(&self) -> PTEFlags {
        PTEFlags::from_bits(self.bits as u8).unwrap()
    }
}
```

PTE 布局：`bits[53:10]` 存 PPN，`bits[7:0]` 存标志位。

---

### 1.4 实现物理帧的管理与分配

**（1）设置物理内存边界**

在 `os/src/config.rs` 中定义：

```rust
pub const MEMORY_END: usize = 0x88000000; // 128MiB
```

物理内存范围为 `[0x80000000, 0x88000000)`，OpenSBI 固件占用前约 2MiB，内核占用区域由链接脚本 `ekernel` 导出符号确定。

**（2）实现栈式帧分配器**

新建 `os/src/mm/frame_allocator.rs`（实际项目为 `frame.rs`）：

```rust
pub struct FrameAllocator {
    current: usize,       // 当前未分配的最小页号
    end: usize,           // 物理内存结束页号
    recycled: Vec<usize>, // 已回收的空闲页号栈
}
```

分配策略：
1. 优先从 `recycled` 弹出已回收的页
2. 否则从 `current` 顺序分配（`current += 1`）
3. `current == end` 时返回 `None`（耗尽）

回收时执行有效性检查：页号不能超过 `current`、不能重复回收。

**（3）全局帧分配器**

通过 `lazy_static!` 创建全局单例，包裹在 `UPSafeCell` 中：

```rust
lazy_static! {
    pub static ref FRAME_ALLOCATOR: FrameAllocatorImpl = unsafe {
        UPSafeCell::new(FrameAllocator::new())
    };
}
```

初始化时从 `ekernel + heap_pages` 到 `MEMORY_END` 设定可分配范围。

**（4）实现 UPSafeCell 同步原语**

新建 `os/src/sync/up.rs`（实际项目为 `os/src/sync/mod.rs` 内联），在单核环境下提供安全的内部可变性：

```rust
pub struct UPSafeCell<T> {
    inner: RefCell<T>,
}
unsafe impl<T> Sync for UPSafeCell<T> {}
```

---

### 1.5 多级页表管理

**（1）实现页表结构**

```rust
pub struct PageTable {
    root_ppn: PhysPageNum,         // 根页表物理页号
    frames: Vec<PhysPageNum>,      // 所有分配的中间页表帧
}
```

**（2）实现页表遍历与创建**

`find_pte_create(vpn)` 方法实现 Sv39 三级页表遍历：
1. 从根页表 `root_ppn` 开始
2. 按 `indexes()[0]` (VPN[2]) → `indexes()[1]` (VPN[1]) → `indexes()[2]` (VPN[0]) 逐级查找
3. 若中间页表不存在，自动分配新帧并创建 PTE
4. 返回叶子 PTE 的可变引用

**（3）实现映射与取消映射**

```rust
pub fn map(&mut self, vpn: VirtPageNum, ppn: PhysPageNum, flags: PTEFlags) {
    let pte = self.find_pte_create(vpn).unwrap();
    *pte = PageTableEntry::new(ppn, flags | PTEFlags::V);
}

pub fn unmap(&mut self, vpn: VirtPageNum) {
    let pte = self.find_pte_create(vpn).unwrap();
    *pte = PageTableEntry::empty();
}
```

**（4）实现 2MB 大页映射**

对大范围连续物理内存（如帧分配器管理的区域），使用 Sv39 大页优化：

```rust
pub fn map_huge(&mut self, vpn: VirtPageNum, ppn: PhysPageNum, flags: PTEFlags) {
    // 直接设置 level-1 PTE 为叶子页，映射 2MB 连续物理内存
    // ppn[0] = 0（2MB 对齐），ppn[1..2] 正常编码
}
```

**（5）实现地址翻译辅助函数**

```rust
pub fn translated_byte_buffer(token: usize, ptr: *const u8, len: usize) -> Vec<&'static mut [u8]> {
    // 根据 satp token 重建临时页表，逐页翻译用户虚拟地址
    // 返回内核可访问的物理页字节切片列表
}
```

---

### 1.6 内核与应用的地址空间

**（1）实现逻辑段 MapArea**

新建 `os/src/mm/map_area.rs`（实际项目），描述一段连续虚拟地址的映射关系：

```rust
pub struct MapArea {
    pub vpn_range: VPNRange,
    pub map_type: MapType,
    pub map_perm: PTEFlags,
}
```

包含三种映射类型：
| MapType | 说明 |
|---------|------|
| `Identical` | 恒等映射：VPN = PPN（内核使用） |
| `Framed` | 按需分配新物理帧（用户应用使用） |
| `Fixed(PhysPageNum)` | 映射到指定物理帧（TrapContext 使用） |

**（2）实现地址空间 MemorySet**

新建 `os/src/mm/memory_set.rs`：

```rust
pub struct MemorySet {
    pub page_table: PageTable,
    pub areas: Vec<MapArea>,
}
```

核心方法：
- `push(area, data)`：添加映射区域并创建页表映射，可选拷贝初始数据
- `push_trampoline(frame, va)`：映射跳板页面
- `satp()`：返回 Sv39 页表基址寄存器值 `(8 << 60) | root_ppn`

**（3）实现内核地址空间**

`init_kernel_memory()` 构建内核地址空间，所有内核区域使用恒等映射：

```
内核虚拟地址空间布局:
┌──────────────────────────┐ 0xFFFF_FFFF_FFFF_F000
│  Trampoline 跳板页       │ 映射到独立物理帧 (R|X)
├──────────────────────────┤
│         ...              │
├──────────────────────────┤ MEMORY_END (0x88000000)
│  帧分配器范围             │ 恒等映射: 2MB大页 + 4KB补齐 (R|W)
├──────────────────────────┤ frame_start
│  内核堆区域               │ 恒等映射 (R|W)
├──────────────────────────┤ ekernel
│  .bss 段                 │ 恒等映射 (R|W)
├──────────────────────────┤
│  .data 段                │ 恒等映射 (R|W)
├──────────────────────────┤
│  .rodata 段              │ 恒等映射 (R|X)
├──────────────────────────┤
│  .text 段                │ 恒等映射 (R|W|X)
└──────────────────────────┘ 0x80200000
```

其中帧分配器范围的主体部分使用 **2MB 大页**优化，减少 TLB 压力。

**（4）实现应用地址空间**

通过解析 ELF 文件构建：

```rust
pub fn from_elf(elf_data: &[u8], app_id: usize, ...) -> (Self, usize, usize) {
    // 1. 解析 ELF 头，遍历所有 LOAD 段
    // 2. 为每段创建 Framed 映射区域，拷贝数据
    // 3. 映射用户栈（Framed, R|W|U）
    // 4. 映射 TrapContext 页（Fixed 到内核栈顶页）
    // 5. 映射 Trampoline 跳板页
    // 返回 (地址空间, 用户栈顶, 入口点)
}
```

用户地址空间布局：

```
┌──────────────────────────┐ 0xFFFF_FFFF_FFFF_F000
│  Trampoline (R|X)        │ 与内核共享同一物理帧
├──────────────────────────┤ 0xFFFF_FFFF_FFFF_E000
│  TrapContext (R|W)       │ 映射到内核栈顶页
├──────────────────────────┤
│         ...              │
├──────────────────────────┤ 0x80400000 + APP_SIZE + USER_STACK
│  User Stack (R|W|U)      │ Framed, 16KB
├──────────────────────────┤ 0x80400000 + APP_SIZE
│  User Code+Data (U)      │ Framed, 从 ELF 加载
└──────────────────────────┘ 0x80400000
```

---

### 1.7 实现基于地址空间的分时多任务

**（1）扩展 TrapContext**

在 `os/src/trap/context.rs` 中增加内核相关字段：

```rust
pub struct TrapContext {
    pub x: [usize; 32],          // 通用寄存器
    pub sstatus: usize,          // [32] 特权级状态
    pub sepc: usize,             // [33] 异常返回地址
    pub kernel_satp: usize,      // [34] 内核页表 satp
    pub kernel_sp: usize,        // [35] 内核栈指针
    pub trap_handler: usize,     // [36] trap_handler 地址
    pub user_satp: usize,        // [37] 用户页表 satp
    pub trap_cx_user: usize,     // [38] 用户态 TrapContext VA
    pub trap_cx_kernel: usize,   // [39] 内核态 TrapContext 地址
}
```

共 40 个 `usize` = 320 字节，放在内核栈顶页内（页对齐，4KB 保护空间）。

**（2）实现跳板机制（Trampoline）**

跳板页面 `trap.S` 放置在独立段 `.text.trampoline`，通过链接脚本固定到代码段的一个对齐页面。该物理帧同时映射到内核和用户地址空间的最高页（`0xFFFF_FFFF_FFFF_F000`）。

`__alltraps` 陷入流程：
1. `csrrw sp, sscratch, sp` — 原子交换 sp 与 sscratch
2. 保存全部用户寄存器到 TrapContext
3. 预加载 kernel_satp、kernel_sp、trap_handler
4. `fence iorw, iorw` 确保存储可见
5. 切换 satp → 内核地址空间 + `sfence.vma`
6. 切换到内核栈，跳转到 `trap_handler()`

`__restore` 恢复流程：
1. 切换到用户 satp + `sfence.vma`
2. 从 TrapContext 恢复所有寄存器
3. 设置 sscratch 为下次陷阱准备
4. `sret` 返回用户态

**（3）更新任务控制块 TaskControlBlock**

```rust
pub struct TaskControlBlock {
    pub task_status: TaskStatus,
    pub task_cx: TaskContext,       // __switch 上下文
    pub memory_set: MemorySet,      // 用户地址空间（含页表）
    pub trap_cx_ppn: PhysPageNum,   // TrapContext 物理页号
}
```

每个任务创建时：
1. 调用 `MemorySet::from_elf()` 构建地址空间
2. 在内核地址空间中插入内核栈映射
3. 初始化 TrapContext（入口点、用户栈、内核 satp、trap_handler 地址）

**（4）改进陷阱处理**

```rust
pub fn trap_handler(cx: &mut TrapContext) -> &TrapContext {
    // 1. 设置内核态陷阱入口（防止内核态异常递归）
    set_kernel_trap_entry();
    // 2. 根据 scause 分发处理
    match scause.cause() {
        UserEnvCall => syscall(...),       // 系统调用
        StorePageFault => exit_current(),  // 页错误 → 杀进程
        SupervisorTimer => suspend_and_switch(), // 时钟中断 → 调度
    }
    // 3. trap_return() 返回用户态
    trap_return();
}
```

**（5）改进 sys_write**

因地址空间隔离，内核无法直接访问用户态数据。通过 `translated_byte_buffer()` 逐页翻译用户虚拟地址 → 物理地址，再读取打印。

**（6）实现任务上下文切换**

`TaskContext::goto_trap_return(kstack_top)` 设置 `ra = trap_return 地址`，`sp = 内核栈顶`。`__switch` 切换后，通过 `ret` 跳转到 trap_return → trampoline → __restore → 用户态。

---

### 1.8 修改应用程序与构建系统

**（1）统一链接脚本**

将 `user/src/linker.ld` 的 `BASE_ADDRESS` 设为 `0x0`（实际项目使用 `0x80400000`），所有应用共享同一链接脚本。

**（2）删除 clear_bss**

用户程序不再需要手动清零 BSS（内核在加载时已清零分配的帧）。

**（3）更新 build.rs**

自动扫描 `user/src/bin/` 目录，将编译后的 ELF 文件通过 `.incbin` 嵌入内核镜像的 `.data` 段，并导出 `_num_app` 符号和地址对。

---

## 二、思考问题

### （1）分析虚拟地址和物理地址的设计与实现

虚拟地址和物理地址的设计采用 **newtype 模式**，核心思想是**类型安全**和**语义明确**。

**类型层次**：

| 类型 | 含义 | 进制关系 |
|------|------|---------|
| `PhysAddr` / `VirtAddr` | 字节级地址 | `phys_addr / PAGE_SIZE = phys_page_num` |
| `PhysPageNum` / `VirtPageNum` | 页号级地址 | `page_num × PAGE_SIZE = addr` |

**关键设计决策**：

1. **零成本抽象**：所有类型都是 `usize` 的 newtype 包装，编译后无运行时开销
2. **编译期安全**：通过 Rust 类型系统防止地址和页号的混淆使用（如误将虚拟地址当作物理地址传给 DMA）
3. **双向转换**：`From<usize>` 和 `From<T> for usize` 提供便捷的 `into()` 和 `from()` 调用
4. **页对齐语义**：`VirtAddr → VirtPageNum` 要求调用 `floor()`（可能非对齐），而 `Trait From` 实现要求严格对齐（`assert page_offset == 0`），在不同场景使用不同接口避免错误
5. **恒等映射下的直接内存访问**：内核使用恒等映射（VA == PA），`PhysPageNum::get_bytes_array()` 直接将物理页号当作虚拟地址指针解引用，这是在内核中访问任意物理内存的基础

**Sv39 地址转换流程**：虚拟地址 39 位 → VPN[2]:VPN[1]:VPN[0] (每级 9 位) + 12 位页内偏移 → 遍历三级页表 → PPN + 偏移 → 物理地址 56 位。

---

### （2）分析物理帧是如何管理与分配的

物理帧管理器采用**栈式分配器**（Stack-based Frame Allocator），兼顾简单性和回收能力。

**数据结构**：

```
FrameAllocator {
    current: usize,       // 未分配的最小页号（单调递增）
    end: usize,           // 物理内存结束页号
    recycled: Vec<usize>, // 已回收页号栈（LIFO）
}
```

**分配算法**：
1. 优先弹出 `recycled` 栈顶 — O(1) 时间实现回收页的快速重用
2. 否则递增 `current` 返回新页 — O(1) 时间
3. `current == end` 时返回 `None` — 耗尽

**回收算法**：
1. 有效性检查：页号必须 `< current` 且不在 `recycled` 中
2. 压入 `recycled` 栈

**内存布局**：帧分配器管理从 `ekernel + heap_pages` 到 `MEMORY_END (0x88000000)` 的物理内存范围，约为 127MiB，约 32600 个 4KB 物理页帧。

**生命周期管理**：本项目直接返回 `PhysPageNum` 而非 RAII 包装器（如 `FrameTracker`），手动调用 `frame_dealloc()` 回收。页表的 `frames: Vec<PhysPageNum>` 保存分配的中间页表帧，在 `PageTable` 被 drop 时自动回收。

**2MB 大页优化**：对帧分配器管理范围的主体部分不逐页映射，而是使用 Sv39 2MB 大页。每个大页只需 1 个 level-1 PTE 而非 512 个 level-0 PTE，大幅减少页表内存开销和 TLB 未命中。

---

### （3）分析内核的地址空间以及应用程序的地址空间是如何实现的

**内核地址空间**使用**恒等映射**（Virtual Addr == Physical Addr），通过 `MapType::Identical` 实现。构建流程：

1. **映射内核段**：`.text` (R|W|X) → `.rodata` (R|X) → `.data` (R|W) → `.bss` (R|W)，全部恒等映射
2. **映射堆区域**：`ekernel` 到 `frame_start`，恒等映射 (R|W)
3. **映射帧分配器范围**：混合策略 — 非对齐头部用 4KB 页，对齐主体用 2MB 大页 (R|W)
4. **映射跳板页面**：将 trampoline 汇编代码所在的独立物理帧映射到虚拟地址 `0xFFFF_FFFF_FFFF_F000`

恒等映射的优势：内核可以直接通过物理地址访问任意内存，无需额外地址转换，简化了物理内存管理和设备 MMIO 访问。

**应用地址空间**使用**框架映射**（`MapType::Framed`），每次映射分配新的物理帧。构建流程：

1. **解析 ELF**：遍历程序头表，为每个 `PT_LOAD` 段创建 Framed 映射区域，将文件数据拷贝到分配的帧
2. **映射用户栈**：在代码段之后分配 16KB 的 Framed 区域 (R|W|U)
3. **映射 TrapContext 页**：使用 `MapType::Fixed` 映射到与内核栈顶共享的物理页 (R|W)
4. **映射 Trampoline 页**：与内核共享同一物理帧 (R|X)

**隔离机制**：
- 每个应用拥有独立的 `MemorySet`（含独立页表），通过切换 `satp` 寄存器实现地址空间隔离
- 用户态页表不包含内核内存映射（除 Trampoline 和 TrapContext 两个共享页外）
- 应用无法访问其他应用或内核的数据，保证了多任务内存安全

---

### （4）分析基于地址空间的分时多任务是如何实现的

基于地址空间的分时多任务将**内存隔离**和**时间片轮转**结合，实现了完整的进程抽象。

**核心组件交互**：

```
应用 A 执行 (U-mode, satp=A)
    │
    │ 10ms 时钟中断
    ▼
__alltraps (Trampoline 页, 用户/内核均映射)
    │ 保存寄存器 → 切内核 satp → 切内核栈
    ▼
trap_handler() (S-mode, satp=kernel)
    │ SupervisorTimer → set_next_trigger → suspend_current
    ▼
__switch(taskA.cx, taskB.cx)
    │ 保存 ra,sp,s0-s11 → 恢复 taskB 的上下文
    ▼
trap_return() → __restore (Trampoline 页)
    │ 切用户 satp → 恢复寄存器 → sret
    ▼
应用 B 执行 (U-mode, satp=B)
```

**地址空间切换**发生在 `__restore` 中：`csrw satp, a1; sfence.vma`。satp 值来自当前任务的 `MemorySet.token()`，即 `(8 << 60) | root_ppn`。

**TrapContext 布局**：320 字节的 TrapContext 放在内核栈顶页（`kstack_top - PAGE_SIZE` 处），留有约 3.7KB 保护空间防止栈溢出覆盖。`kernel_sp = kstack_top`，`kernel_satp` 指向内核页表，`trap_handler` 指向 Rust 的 `trap_handler()` 函数。

**任务调度流程**：
1. `run_first_task()`：构建 `TaskContext::goto_trap_return()`，`__switch` 后进入第一个任务
2. 时钟中断 → `mark_current_suspended()` → `find_next_task()` (round-robin) → `__switch`
3. 应用 exit → `mark_current_exited()` → 找下一个 Ready 任务
4. 所有任务 Exited → panic!("All applications completed!")

**与无地址空间版本的区别**：
- 旧版：所有应用共享同一物理内存，通过不同起始地址区分
- 新版：每个应用拥有独立页表和地址空间，`0x80400000` 对每个应用指向不同的物理帧，实现真正的隔离

---

### （5）编写新的应用程序并测试验证结果

运行 `make run`，四个测试程序全部成功：

```
Power_3 test 0: val = 1
Power_3 test 1: val = 3
...
Power_3 test 99: val = 17451869791863703195
Power_3 OK!
[kernel] App exited with code=0

Power_5 test 0: val = 1
...
Power_5 test 99: val = 10299458830999786749
Power_5 OK!
[kernel] App exited with code=0

Power_7 test 0: val = 1
...
Power_7 test 99: val = 532636070097922135
Power_7 OK!
[kernel] App exited with code=0

[sleep] start at 66391
[sleep] end at 256543, duration = 190152ms
[kernel] App exited with code=0
[kernel] Panic at src/task/mod.rs: All applications completed!
```

三个 power 测试的输出交错出现（被时钟中断打断后切换到其他任务），验证了抢占式调度的正确性。sleep 测试通过 `sys_get_time` 测量实际等待时间约 190 秒（因 QEMU 无加速时的模拟时间），验证了时钟中断和系统调用均正常工作。

---

## 四、其他说明（遇到的问题及解决方案）

### 问题 1：`_start` 不是 ELF 镜像的第一个符号导致 syscall_id=0

**现象**：内核启动第一个用户程序时，`trap_handler` 收到 `syscall_id=0`，`sepc=0x80400010`，系统 panic。

**原因**：`entry.S` 中 `_user_syscall` 和 `_start` 都在 `.section .text.entry` 段中，且 `_user_syscall` 在源文件中声明在前。汇编器按声明顺序排放代码，导致 `_user_syscall` 的指令码位于镜像起始地址 `0x80400000`——恰好是内核设置的入口点。CPU 执行 `_user_syscall` 时，寄存器 a7=0（初始值），ecall 发出 `syscall(0, ...)`。

**解决**：将 `_start` 移到独立的 `.section .text.start` 段，并确保链接脚本中 `*(.text.start)` 排在 `*(.text.entry)` 之前。利用链接器字典序保证 `_start` 始终是镜像第一个符号。

---

### 问题 2：TrapContext 被内核栈溢出覆盖

**现象**：进入用户态后，TrapContext 的关键字段（如 `kernel_sp`）被错误值覆盖，导致 trap_handler 返回时内核栈指针异常。

**原因**：早期设计将 TrapContext（320 字节）紧贴内核栈顶放置（偏移量 = 320）。当 `trap_handler` 中使用较深的调用栈（如打印调试信息、多层函数调用）时，栈帧向下溢出，覆盖了 TrapContext 的数据。

**解决**：将 TrapContext 放置位置修改为 `kstack_top - PAGE_SIZE`，即独占内核栈顶部的一个完整 4KB 页。页对齐后留有约 3776 字节的保护空间，确保即使 `trap_handler` 使用较深的调用栈，也不会覆盖 TrapContext。

---

### 问题 3：构建系统 ELF 解析与内存布局

**现象**：用户程序 ELF 文件中包含多个 `PT_LOAD` 段（如 `.text` 和 `.rodata` 分离），直接按段的原始虚拟地址加载会导致地址空间不连续。

**原因**：Rust 编译器可能将只读数据（`.rodata`）和代码（`.text`）放在不同的 LOAD 段，它们的文件偏移和虚拟地址可能不一致。直接拷贝文件数据到虚拟地址时，需要处理段间的间隙。

**解决**：使用 `xmas-elf` 库解析 ELF 格式，遍历所有 `PT_LOAD` 段，为每个段创建对应的 `MapArea`（`MapType::Framed`），并通过 `MapArea::copy_data()` 将文件数据按页拷贝到分配的物理帧中。

---

### 问题 4：链接脚本中段顺序与符号导出

**现象**：内核初始化时打印的各段地址范围异常，如 `.bss` 起始地址低于 `.data` 结束地址。

**原因**：链接脚本中 `.text.trampoline` 段的对齐要求（`ALIGN(4K)`）可能影响后续段的布局。同时，在 `.bss` 段之后声明的新符号（如 `ekernel`）需要通过 `PROVIDE` 或 `. = ALIGN(4K)` 确保正确的地址计算。

**解决**：精心设计链接脚本的段布局，确保 `stext < etext < srodata < erodata < sdata < edata < sbss < ebss < ekernel` 的严格顺序。通过 `extern "C"` 导入这些符号，在 Rust 侧通过 `as *const () as usize` 获取实际地址。

---

### 经验总结

1. **汇编段顺序至关重要**：在裸机/微内核开发中，链接器对段的排列直接影响入口点。必须确保 `_start` 是镜像的第一个可执行指令
2. **栈保护是必需的**：在资源受限的内核环境中，TrapContext 等关键数据结构必须有足够的保护空间，避免栈溢出破坏
3. **恒等映射简化内核设计**：内核使用 VA == PA 的恒等映射，使得物理内存访问无需额外的地址转换，这在 boot 阶段和早期初始化中尤其重要
4. **大页映射值得实现**：对于数百 MB 的物理内存，2MB 大页能将页表条目数减少 512 倍，显著降低内存开销和 TLB miss
5. **地址空间隔离是真正的进程抽象基础**：从简单分区到独立页表，是操作系统从"任务"到"进程"的关键一步
