# 实验报告：进程及进程管理

---

## 一、实验步骤

### 1.1 修改应用程序——增加系统调用

在 `user/src/syscall.rs` 中增加 `fork`、`waitpid`、`getpid`、`exec`、`read` 系统调用号及封装函数（`SYSCALL_READ=63`, `SYSCALL_GETPID=172`, `SYSCALL_FORK=220`, `SYSCALL_EXEC=221`, `SYSCALL_WAITPID=260`），并在 `user/src/lib.rs` 中封装为用户友好接口，其中 `wait()` 在子进程未退出时（`-2`）主动 `yield_()` 让出 CPU。

---

### 1.2 实现用户初始程序 initproc

`user/src/bin/initproc.rs` 是系统第一个用户进程：fork 出子进程执行 `user_shell`，自身循环 `wait()` 回收僵尸子进程。

---

### 1.3 实现 shell 程序

- **`user/src/console.rs`**：基于 `sys_read` 从标准输入读取单字符
- **`user/src/bin/user_shell.rs`**：交互式命令行，支持 `fork()` + `exec()` 执行应用、退格键删除字符、`String` 类型（需动态内存）
- **用户态动态内存分配**：在 `user/src/lib.rs` 中配置 `buddy_system_allocator::LockedHeap`，`USER_HEAP_SIZE=16384`，在 `_start()` 中初始化

---

### 1.4 在内核中增加系统调用

修改 `os/src/syscall/mod.rs` 增加分发逻辑，新建 `os/src/syscall/fs.rs`（`sys_read`：从 SBI 读取字符，无输入时挂起当前任务）和 `os/src/syscall/process.rs`：

| 系统调用 | 核心逻辑 |
|---------|---------|
| `sys_getpid` | 返回 `current_task().pid.0` |
| `sys_fork` | 调用 `current_task.fork()` 创建子进程，修改子进程 TrapContext `x[10]=0`，加入调度器 |
| `sys_exec` | 通过路径名查找 ELF 数据，调用 `task.exec(data)` 替换地址空间 |
| `sys_waitpid` | 在父进程 `children` 列表中查找：不存在返回 -1，未退出返回 -2，已退出则移除并返回 exit_code |

---

### 1.5 应用的链接与加载（基于名字）

**`os/build.rs`**：链接脚本中导出 `_app_names` 符号，嵌入应用名字符串。
**`os/src/loader.rs`**：用 `lazy_static!` 创建 `APP_NAMES` 向量从符号解析名字，实现 `get_app_data_by_name(name)` 按名查找 ELF 数据。

---

### 1.6 进程标识符与内核栈

- **PidHandle**（`os/src/task/pid.rs`）：newtype 包装，`PidAllocator` 基于单调递增 + 回收栈分配 PID，`Drop` 自动回收
- **KernelStack**：通过 `kernel_stack_position(pid)` 计算内核栈虚拟地址（`top = TRAMPOLINE - pid * (KERNEL_STACK_SIZE + PAGE_SIZE)`），创建时插入映射，`Drop` 时移除并回收物理帧

---

### 1.7 修改进程控制块

升级 `TaskControlBlock` 为完整 PCB：

- **外层不可变**：`pid: PidHandle`、`kernel_stack: KernelStack`
- **内层可变**（`UPSafeCell`）：`trap_cx_ppn`、`base_size`、`task_cx`、`task_status`、`memory_set`、`parent: Weak<...>`、`children: Vec<Arc<...>>`、`exit_code`

三种状态：`Ready → Running → Zombie`。核心方法：`new(elf_data)`、`exec(elf_data)`、`fork()`。

---

### 1.8 实现任务管理器

`os/src/task/manager.rs`：`VecDeque<Arc<TaskControlBlock>>` 实现 FIFO 调度队列，通过 `lazy_static!` + `UPSafeCell` 创建全局单例 `TASK_MANAGER`。

---

### 1.9 增加处理器管理结构

`os/src/task/processor.rs` 分离 CPU 状态维护：

| 函数 | 功能 |
|------|------|
| `run_tasks()` | 调度循环：取任务 → `__switch` 切入 |
| `schedule(cx_ptr)` | 保存上下文 → `__switch` 回 idle |
| `take_current_task()` | 取出当前任务所有权 |
| `current_task()` | 克隆当前任务 Arc |
| `current_user_token()` | 获取用户页表 token |
| `current_trap_cx()` | 获取 TrapContext 可变引用 |

---

### 1.10 创建初始进程

`os/src/task/mod.rs` 中用 `lazy_static!` 创建 `INITPROC`，`main.rs` 中调用 `add_initproc()` 加入调度器后再启动 `run_tasks()`。

---

### 1.11 进程调度机制

`suspend_current_and_run_next()` 实现抢占式调度：取出当前任务 → 设 Ready → 放回队列 → `schedule()`。时钟中断触发 `SupervisorTimer` → 调用此函数，实现时间片轮转。

---

### 1.12 进程的生成机制（fork + exec）

**fork**：`MemorySet::from_existed_user()` 遍历父进程所有 `MapArea`，逐页分配新物理帧并拷贝数据。`TaskControlBlock::fork()` 拷贝地址空间 → 分配 PID/内核栈 → 设父子关系（父用 `Weak` 避免循环引用）。

**exec**：`TaskControlBlock::exec(elf_data)` 用 `MemorySet::from_elf()` 构建新地址空间，替换 memory_set 和 trap_cx_ppn，重新初始化 TrapContext。

**注意**：`syscall` 返回后需重新 `cx = current_trap_cx()`，因为 exec 可能使旧引用失效。

辅助函数：`PageTable::translate_va()`、`translated_str(token, ptr)`、`translated_refmut(token, ptr)`、`PhysAddr::get_mut<T>()`。

---

### 1.13 进程资源回收机制

**退出**：`exit_current_and_run_next(exit_code)` 设 Zombie → 子进程过继给 `INITPROC` → 回收用户空间物理页 → `schedule()`。

**回收地址空间**：`MemorySet::recycle_data_pages()` 清空 areas 列表，触发各 MapArea 的 drop，回收所有物理帧。

**父进程回收**：`sys_waitpid()` 在 children 中查找僵尸子进程，移除并释放最后的 Arc 引用。

---

## 二、思考问题

### （1）分析应用的链接与加载是如何实现的

**链接**：用户程序统一使用 `user/linker.ld`（`BASE_ADDRESS=0x80400000`），`os/build.rs` 编译各应用并通过 `.incbin` 嵌入内核 `.data` 段，同时导出 `_num_app`、`_app_names` 等符号。

**加载**：`loader.rs` 从符号解析名字列表，`get_app_data_by_name(name)` 按名查找 ELF 数据。`MemorySet::from_elf()` 用 `xmas-elf` 库解析 PT_LOAD 段，按页分配物理帧并拷贝数据，追加用户栈、TrapContext 页和 Trampoline 页。

**与旧版区别**：旧版只能按索引加载；新版支持按名字查找，可通过 exec 在运行时动态加载任意应用。

---

### （2）分析进程标识符、进程控制块是如何设计和实现的

**PidHandle**：newtype 模式，`PidAllocator` 单调递增分配 + 回收栈复用（O(1)），`Drop` 自动回收，全局单例保证线程安全。

**TaskControlBlock**：内外分离——外层不可变字段（pid、kernel_stack），内层用 `UPSafeCell` 包裹可变状态。父进程用 `Weak` 防止循环引用；三种状态（Ready/Running/Zombie）；与旧版相比新增 PID、父子关系、地址空间、退出码等完整进程语义。

---

### （3）分析任务管理是如何实现的

两层设计：

- **TaskManager**：`VecDeque<Arc<TaskControlBlock>>` FIFO 就绪队列，全局单例
- **Processor**：维护 `current` 任务和 `idle_task_cx` 空闲上下文

调度流程：`run_tasks()` 取任务 → `__switch` 切入 → 任务执行（时钟中断/trap）→ `suspend_current_and_run_next()` 设 Ready 放回队列 → `__switch` 回 idle → 循环。两层分离使调度策略与 CPU 状态切换职责清晰，可独立替换。

---

### （4）分析进程的调度、生成、以及进程资源的回收是如何实现的

**调度**：抢占式轮转，时钟中断触发 `trap_handler` → `suspend_current_and_run_next()`，所有进程公平获得相同时间片。

**生成**：两条路径——initproc 由内核手动创建；fork+exec 是 UNIX 经典二阶段模式（fork 拷贝资源 + exec 替换执行），将进程创建与程序加载解耦。

**回收**：两层机制——进程退出时进入 Zombie（不立即释放所有资源），父进程通过 `waitpid` 回收；父进程先于子进程退出时，子进程过继给 initproc 防止孤儿进程。`exit_code` 存储在 Zombie PCB 中。`Arc<usize>` 引用计数自动管理生命周期，最终 Drop 链释放内核栈帧和 PID。

---

## 三、其他说明（遇到的问题及解决方案）

### 问题 1：sstatus.FS 字段丢失导致用户态 FP 指令触发 IllegalInstruction

**现象**：fork 后子进程执行 FP 指令触发 `IllegalInstruction`。

**原因**：RISC-V `sstatus.FS` 控制 FPU 状态。QEMU 的 `sret` 从 S-mode 跳到 U-mode 时会将 `FS=Initial(01)` 清零为 `FS=Off(00)`，导致用户态 FP 指令异常。而 `FS=Dirty(11)` 可被保留。

**解决**：将 `app_init_context` 和 `trap_handler` 返回前的 FS 设置从 `Initial(01)` 改为 `Dirty(11)`。

---

### 问题 2：sys_exec 后 TrapContext 引用失效

**原因**：`exec` 替换整个地址空间（含 TrapContext 页），旧引用指向已释放的物理帧。

**解决**：系统调用返回后重新调用 `current_trap_cx()` 获取新引用。

---

### 问题 3：内核态与用户态 trap 的处理分离

**原因**：原 `__alltraps` 假定所有 trap 来自用户态，引入进程后内核态异常需不同处理路径。

**解决**：在 `trap.S` 中检测 sstatus.SPP 位——SPP=0（用户态）走正常保存流程，SPP=1（内核态）走快速路径。

---

### 经验总结

1. **RISC-V 特权级规范需仔细研读**：`sstatus.FS` 的行为差异说明阅读硬件规范细节的重要性
2. **fork 二阶段设计是经典范式**：fork 复制资源 + exec 替换执行，将进程创建与程序加载解耦
3. **父子进程关系的生命周期管理**：`Weak` 避免循环引用，Zombie 状态实现异步回收，initproc 作为孤儿进程收容所
4. **地址空间隔离是进程安全的基础**：每个进程拥有独立页表，只有系统调用才能跨越隔离边界
5. **帧分配器 + 页表 + 地址空间三层架构**：物理帧管理 → 虚拟地址映射 → 逻辑段组织，层层抽象，职责分明
