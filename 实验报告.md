# 实验报告：分时多任务与抢占式调度操作系统

---

## 一、实验步骤

### 1.1 时钟中断与计时器

**（1）新建 `os/src/timer.rs` 子模块**

实现三个核心函数：
- `get_time()` — 读取 RISC-V `mtime` 寄存器，获取当前时钟计数
- `get_time_ms()` — 转换为毫秒单位返回
- `set_next_trigger()` — 设置下一次时钟中断触发时间（当前时间 + `CLOCK_FREQ / 100`，即 10ms 后）

```rust
const TICKS_PER_SEC: usize = 100;
const MSEC_PER_SEC: usize = 1000;

pub fn get_time() -> usize { time::read() }
pub fn get_time_ms() -> usize { time::read() / (CLOCK_FREQ / MSEC_PER_SEC) }
pub fn set_next_trigger() { set_timer(get_time() + CLOCK_FREQ / TICKS_PER_SEC); }
```

**（2）扩展 `os/src/sbi.rs`**

增加向 OpenSBI 发起 `SBI_SET_TIMER` 调用，写入 `mtimecmp` 寄存器：

```rust
const SBI_SET_TIMER: usize = 0;
pub fn set_timer(timer: usize) { sbi_call(SBI_SET_TIMER, timer, 0, 0); }
```

**（3）修改 `os/src/config.rs`**

添加 CPU 时钟频率常量：

```rust
pub const CLOCK_FREQ: usize = 12500000;  // QEMU riscv64 virt 默认频率 12.5MHz
```

### 1.2 增加 `get_time` 系统调用

**（1）内核侧 `os/src/syscall/process.rs`**

```rust
pub fn sys_get_time() -> isize { get_time_ms() as isize }
```

**（2）内核侧 `os/src/syscall/mod.rs`**

注册系统调用号 169：

```rust
const SYSCALL_GET_TIME: usize = 169;
SYSCALL_GET_TIME => sys_get_time(),
```

**（3）用户侧 `user/src/syscall.rs` 和 `user/src/lib.rs`**

分别增加 `sys_get_time()` 底层调用和 `get_time()` 用户库封装。

### 1.3 实现测试应用程序

在 `user/src/bin/` 下创建四个测试程序：

| 应用 | 功能 | 入口地址 |
|------|------|---------|
| `00power_3` | 计算 `3^200000 mod 998244353`，每 10000 次打印进度 | `0x80400000` |
| `01power_5` | 计算 `5^200000 mod 998244353`，每 10000 次打印进度 | `0x80420000` |
| `02power_7` | 计算 `7^200000 mod 998244353`，每 10000 次打印进度 | `0x80440000` |
| `03sleep` | 通过 `get_time()` + `yield_()` 实现 3000ms 等待 | `0x80460000` |

并更新 `user/Cargo.toml` 注册这四个 `[[bin]]` 条目。

### 1.4 实现抢占式调度

**（1）修改 `os/src/trap/mod.rs`**

核心改动：在 `trap_handler` 中增加时钟中断分支，实现强制切换：

```rust
use riscv::register::scause::{Interrupt};
use crate::timer::set_next_trigger;

pub fn enable_timer_interrupt() {
    unsafe { sie::set_stimer(); }
}

#[no_mangle]
pub fn trap_handler(cx: &mut TrapContext) -> &mut TrapContext {
    match scause.cause() {
        // ... 原有异常处理 ...
        Trap::Interrupt(Interrupt::SupervisorTimer) => {
            set_next_trigger();            // 重新设置下次时钟中断
            suspend_current_and_run_next(); // 挂起当前任务，切换下一个
        }
        // ...
    }
}
```

**（2）修改 `os/src/main.rs`**

在 `rust_main` 中，于首个应用执行前添加初始化：

```rust
mod timer;                        // 新增模块声明
trap::enable_timer_interrupt();   // 开启 S 态时钟中断使能
timer::set_next_trigger();        // 设定首次触发时间
```

调度流程：`rust_main → 开中断 → 设首次触发 → 加载应用 → run_first_task → __switch → __restore → sret → 用户态执行 → 10ms 后时钟中断 → __alltraps → trap_handler(SupervisorTimer) → 切换 → …`

---

## 二、思考问题

### （1）分析分时多任务是如何实现的

分时多任务通过**时钟中断 + 轮转调度**三层机制实现：

1. **计时器设定**：内核调用 `timer::set_next_trigger()`，通过 SBI 接口向 `mtimecmp` 寄存器写入 `mtime + CLOCK_FREQ/100`，即 10ms 后的时刻
2. **硬件中断**：当 RISC-V 硬件时钟 `mtime` 超过 `mtimecmp` 时，自动产生 `SupervisorTimer` 中断，CPU 从用户态陷入内核态，跳转到 `__alltraps`
3. **调度切换**：`trap_handler` 匹配 `Interrupt::SupervisorTimer` 后：
   - 调用 `set_next_trigger()` 设定下一次中断
   - 调用 `suspend_current_and_run_next()` → `mark_current_suspended()`（当前任务 → Ready）→ `find_next_task()`（轮询 `(current + i) % num_app` 找下一个 Ready）→ `__switch` 切换上下文
4. **循环往复**：新任务执行 10ms 后再次触发中断，如此交替

**时间片长度**：`CLOCK_FREQ / TICKS_PER_SEC = 12500000 / 100 = 125000` 个时钟周期 ≈ 10ms。

### （2）分析抢占式调度是如何设计和实现的

抢占式调度的设计分硬件、内核初始化、中断处理三个层次：

| 层次 | 组件 | 职责 |
|------|------|------|
| **硬件层** | RISC-V `mtime` / `mtimecmp` + `sie.stimer` 使能位 | 硬件持续计时，超时自动产生中断，无需软件干预 |
| **内核初始化** | `trap::enable_timer_interrupt()` + `timer::set_next_trigger()` | 在 `rust_main` 中开启中断使能并写入首次触发时间 |
| **中断处理** | `trap_handler` 匹配 `Interrupt::SupervisorTimer` | 重置下次触发 + 挂起当前任务 + 切换到下一个 Ready 任务 |

**与协作式的本质区别**：调度权完全在内核手中。应用即使不调用 `yield_()`，内核也会在 10ms 后通过硬件中断**强制剥夺 CPU**，保证了"抢占"语义。

### （3）对比上个实验实现的协作式调度与本实验实现的抢占式调度

| 维度 | 协作式调度（上一实验） | 抢占式调度（本实验） |
|------|----------------------|---------------------|
| **切换触发** | 仅应用主动调用 `yield_()` 或 `exit()` | 时钟中断强制切换 + 应用主动让出 |
| **调度权归属** | 应用决定何时放弃 CPU | 内核通过硬件中断控制 |
| **公平性** | 依赖应用自觉，恶意/死循环会饿死其他任务 | 内核保证 10ms 时间片轮转，绝对公平 |
| **响应性** | 差 — 长计算任务完全阻塞其他应用 | 好 — power_3 算到一半即被打断让 power_5 执行 |
| **实现复杂度** | 仅需 syscall 处理 | 额外需要 mtimecmp 配置、sie 中断使能、中断处理分支 |
| **典型行为** | 输出顺序，一个任务完成才轮到下一个 | **输出交错**，三个 power 任务进度交替打印 |

> 本实验运行输出的典型证据：`power_3 [10000/200000]` 之后紧接 `power_5 [10000/200000]`，随后又出现 `power_7 [30000/200000]` — 这是 10ms 时钟中断抢占的直接效果。

---

## 三、（待定）

---

## 四、其他说明（遇到的问题及解决方案）

### 问题 1：`extern crate user_lib` 找不到 crate

**现象**：新建的四个用户程序编译时报 `can't find crate for user_lib`。

**原因**：`user/Cargo.toml` 中 `[package] name = "user"`，库名是 `user` 而非 `user_lib`。上一实验的应用使用了 `extern crate user_lib`，新应用也跟着写错了。

**解决**：将所有 `extern crate user_lib;` 替换为 `extern crate user;`，`use user_lib::` 替换为 `use user::`。

### 问题 2：03sleep.rs 中 `use user_lib::{get_time, yield_}` 未同步修改

**现象**：用 sed 只替换了 `extern crate user_lib;`，遗漏了 `use user_lib::` 语句，03sleep.rs 仍然编译失败。

**解决**：补充执行 `sed -i 's/use user_lib::/use user::/' user/src/bin/03sleep.rs`。

### 问题 3：数组越界 `index 4 out of len 4`

**现象**：内核成功加载 6 个应用，但 `TASK_MANAGER` 只分配了 4 个 `TaskControlBlock` 槽位，运行时 panic。

**原因**：旧实验的 3 个 `write` 应用没有被删除，加上新的 4 个应用共 7 个（后 fix 为 4+2=6）。Cargo.toml 虽已替换但旧源文件仍存在，用户 Makefile 会扫描全部 `src/bin/*.rs` 编译。

**解决**：手动删除 `user/src/bin/00write_a.rs`、`01write_b.rs`、`02write_c.rs`。

### 问题 4：`sys_exit` 返回类型 `!` 不匹配

**现象**：`os/src/syscall/process.rs` 中 `fn sys_exit() -> !` 但函数体调用 `exit_current_and_run_next()` 后隐式返回 `()`，编译器报 `expected !, found ()`。

**原因**：新版的 syscall/mod.rs 将 exit/yield 处理内联到 match 分支中，`process.rs` 中的 `sys_exit`/`sys_yield` 变为死代码。同时 `sys_exit` 发散的 `!` 返回类型需要显式 `panic!()` 或 `loop {}` 满足。

**解决**：精简 `process.rs`，移除不再使用的 `sys_exit` 和 `sys_yield`，只保留 `sys_get_time`。

### 经验总结

1. **crate 命名一致性**：`extern crate` 和 `use` 中的 crate 名必须与 Cargo.toml `[package] name` 严格一致
2. **构建系统隔离**：修改用户应用时需注意清理旧文件，避免 Makefile 自动扫描引入多余程序
3. **发散函数 (`!`)**：Rust 要求标记 `-> !` 的函数必须通过 `panic!()` / `loop {}` / `unreachable!()` 等确保不返回，这是编译期强制检查
